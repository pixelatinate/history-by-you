# Copy to .env.local (Expo loads EXPO_PUBLIC_* vars automatically) and fill
# in once you've created a Supabase project. Until these are set, the app
# renders MOCK_LOCATIONS from @history-by-you/db instead of a real database.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
